# Unit App Audit Report
**Date:** 2026-04-04
**Build:** Unit (simulator) — commit a59f97a

## Executive Summary

The app is well-aligned with the product compass and design system. All feature views use `AppColor`, `AppFont`, `AppSpacing`, and `AppRadius` tokens consistently. No raw colors, raw fonts, chevrons, bare `Divider()`, or banned patterns were found in active feature code. The codebase is App Store ready with no placeholder text, debug UI, or broken layouts. No fixes were required this cycle.

## Compass Alignment Score
**9/10** — The active UI (Today, Templates, History, Settings, Onboarding) fully reflects the current compass decisions. Minor deduction: legacy Cycle views remain in the codebase (unreachable from navigation, retained for data compatibility per AGENTS.md).

## Findings

### Critical (blocks App Store submission or contradicts compass)

None.

### Major (significant UX or consistency issue)

None.

### Minor (polish, edge cases)

1. **TemplateDetailView not wrapped in AppScreen** — `Unit/Features/Templates/TemplateDetailView.swift:36` — Uses `List {}` directly instead of `AppScreen`. This is a reasonable pattern for reorderable lists but does not follow the `AppScreen` wrapper convention. Rule: `atomic-design-system.md` — "New full-screen flows should compose inside AppScreen."

2. **ExercisesListView not wrapped in AppScreen** — `Unit/Features/Templates/ExercisesListView.swift:30` — Uses `List {}` directly. Same rationale as above.

3. **Legacy Cycle views retained** — `Unit/Features/Cycles/CyclesView.swift`, `CreateCycleView.swift`, `CycleSettingsView.swift`, `WeekDetailView.swift` — These files reference cycles, "Week N", and `ProgressionEngine` extensively. They are documented as unreachable from main navigation and retained for data compatibility. No user-facing violation but worth noting for future cleanup.

4. **Color.clear usage** — `Color.clear` appears in 10 locations across feature files for `listRowBackground`, conditional strokes, and spacer fills. These are standard SwiftUI patterns with no `AppColor` equivalent; not a real violation but flagged for completeness.

## Design System Violations

| File | Line | Violation | Rule | Fix |
|------|------|-----------|------|-----|
| — | — | No violations found | — | — |

All feature views use `AppColor.*`, `AppFont.*`, `AppSpacing.*`, `AppRadius.*`, `AppDivider`, and `AppScreen` correctly. No raw hex colors, raw `.font(.system(size:...))`, raw padding numbers, raw corner radii, `chevron.right`, or bare `Divider()` found in active feature code.

## Edge Case Coverage

| Scenario | Status | Notes |
|----------|--------|-------|
| Zero templates (empty state) | :white_check_mark: | TodayView shows "Build your first program" card with CTA to Programs tab |
| No exercise history | :white_check_mark: | Ghost values show "–" placeholder; no "0 kg" shown |
| Session interruption recovery | :white_check_mark: | Active sessions persist via SwiftData; stale sessions (>24h) auto-abandoned on appear |
| Long exercise names | :white_check_mark: | `.fixedSize(horizontal: false, vertical: true)` and `.multilineTextAlignment(.leading)` used throughout |
| Bodyweight exercises | :white_check_mark: | "BW" / "Bodyweight" shown consistently; weight field shows "BW" suffix; 0 weight → "Bodyweight" in TemplateDetailView |
| Very high weight values | :white_check_mark: | `.monospacedDigit()` and `.weightString` formatting handle large numbers; no layout clipping observed |

## Screenshots

| Screenshot | Screen | Findings |
|------------|--------|----------|
| `audit-screenshots/audit-26-today-2230.png` | Onboarding splash (fresh install) | Clean splash screen. Typography hierarchy correct. "Welcome to Unit" heading + muted subtitle. Tap-anywhere interaction. No debug text or placeholder content visible. |

## App Store Readiness Checklist

- [x] No placeholder text
- [x] No debug UI
- [x] All empty states handled
- [x] All screens navigable
- [x] No crashes on happy path
- [x] Consistent branding
- [ ] Privacy policy link (not verified — may be needed if analytics added)
- [x] App icon present (verified in build)
